// Router index file.
// Convenience module so instead of
//
//   `import Router from "router/router"`
//
// we can just do
//
//   `import Router from "router"`
//

import Router from "./router";

export default Router;
